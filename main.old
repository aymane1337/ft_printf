#include "ft_printf.h"
#include <stdio.h>
#include <limits.h>
#include <unistd.h>

int main(void)
{
    int a;

    // a = printf("%", "hey");
    // printf("Returned -> ft_printf: %d" , a);


    a = printf("%", "hey");
    printf("Returned -> ft_printf: %d" , a);

    return (0);
}
